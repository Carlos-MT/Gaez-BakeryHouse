<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Gaez.BakeryHouse.Controls"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:Class="Gaez.BakeryHouse.Views.HomePageFolder.CourseView">

    <ContentView.Resources>
        <ResourceDictionary>
            <Style TargetType="Frame" x:Key="frameImageStyle">
                <Setter Property="HeightRequest" Value="180"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="BackgroundColor" Value="{StaticResource superLightGrayColor}"/>
                <Setter Property="HasShadow" Value="False"/>
                <Setter Property="Padding" Value="0"/>
            </Style>
        </ResourceDictionary>
    </ContentView.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <controls:CustomLabel Grid.Row="0"
                              Style="{StaticResource labelGenericMediumStyle}"
                              Text="Cursos"/>
        <Frame Grid.Row="1"
               Style="{StaticResource frameImageStyle}"/>
    </Grid>
</ContentView>