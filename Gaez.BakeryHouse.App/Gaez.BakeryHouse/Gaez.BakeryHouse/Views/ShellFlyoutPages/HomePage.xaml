<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Gaez.BakeryHouse.Controls"
             xmlns:fonts="clr-namespace:Gaez.BakeryHouse.Fonts"
             xmlns:bh="clr-namespace:Gaez.BakeryHouse.Behaviors"
             xmlns:vm="clr-namespace:Gaez.BakeryHouse.ViewModels"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:Class="Gaez.BakeryHouse.Views.ShellFlyoutPages.HomePage">

    <ContentPage.BindingContext>
        <vm:HomeViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Frame" x:Key="fimgStyle">
                <Setter Property="HasShadow" Value="False"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="HeightRequest" Value="180"/>
                <Setter Property="BackgroundColor" Value="{StaticResource superLightGrayColor}"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <Shell.TitleView>
        <Frame Margin="0,10,10,10"
               CornerRadius="10"
               HasShadow="False"
               Padding="0">
            <controls:CustomSearchBar Text="{Binding InputText}"
                                      IsEnabled="{Binding IsSearchEnable}">
                <controls:CustomSearchBar.Behaviors>
                    <bh:TextChangedToCommandBehavior Command="{Binding OnSearchCommand}"/>
                </controls:CustomSearchBar.Behaviors>
            </controls:CustomSearchBar>
        </Frame>
    </Shell.TitleView>

    <RefreshView RefreshColor="{StaticResource primaryColor}"
                 IsEnabled="{Binding IsRefreshEnable}"
                 IsRefreshing="{Binding IsRefreshVisible}"
                 Command="{Binding OnRefreshCommand}">
        <ScrollView HorizontalScrollBarVisibility="Never"
                    VerticalScrollBarVisibility="Never"
                    IsEnabled="{Binding IsScrollEnable}">
            <Grid RowDefinitions="Auto,Auto">
                <Grid RowDefinitions="Auto,Auto,Auto,Auto"
                      IsVisible="{Binding IsContentViewVisible}"
                      RowSpacing="20"
                      Margin="10">
                    <StackLayout Grid.Row="0"
                                 Orientation="Vertical">
                        <StackLayout Orientation="Horizontal">
                            <controls:CustomLabel Style="{StaticResource labelGenericMediumStyle}"
                                   HorizontalOptions="StartAndExpand"
                                   VerticalOptions="End"
                                   Text="Cursos"/>
                            <controls:CustomLabel Style="{StaticResource labelGenericSmallStyle}"
                                   HorizontalOptions="EndAndExpand"
                                   VerticalOptions="End"
                                   TextColor="{StaticResource primaryColor}"
                                   Text="Ver más"/>
                        </StackLayout>
                        <Frame Style="{StaticResource fimgStyle}"/>
                    </StackLayout>
                    <StackLayout Grid.Row="1"
                                 Orientation="Vertical">
                        <StackLayout Orientation="Horizontal">
                            <controls:CustomLabel Style="{StaticResource labelGenericMediumStyle}"
                                   HorizontalOptions="StartAndExpand"
                                   VerticalOptions="End"
                                   Text="Ofertas especiales"/>
                            <controls:CustomLabel Style="{StaticResource labelGenericSmallStyle}"
                                                  HorizontalOptions="EndAndExpand"
                                                  VerticalOptions="End"
                                                  TextColor="{StaticResource primaryColor}"
                                                  Text="Ver más"/>
                        </StackLayout>
                        <Grid ColumnDefinitions="*,*"
                              ColumnSpacing="10">
                            <Frame Grid.Column="0"
                                   Style="{StaticResource fimgStyle}"/>
                            <Frame Grid.Column="1" 
                                   Style="{StaticResource fimgStyle}"/>
                        </Grid>
                    </StackLayout>
                    <StackLayout Grid.Row="2"
                                 Orientation="Vertical">
                        <StackLayout Orientation="Horizontal">
                            <controls:CustomLabel Style="{StaticResource labelGenericMediumStyle}"
                                   HorizontalOptions="StartAndExpand"
                                   VerticalOptions="End"
                                   Text="Recetas"/>
                            <controls:CustomLabel Style="{StaticResource labelGenericSmallStyle}"
                                                  TextColor="{StaticResource primaryColor}"
                                                  HorizontalOptions="EndAndExpand"
                                                  VerticalOptions="End"
                                                  Text="Ver más"/>
                        </StackLayout>
                        <Frame Style="{StaticResource fimgStyle}"/>
                    </StackLayout>
                </Grid>
                <CollectionView Grid.Row="0"
                                x:Name="cv"
                                Grid.RowSpan="2"
                                ItemsSource="{Binding ProductCollection}"
                                IsVisible="{Binding IsSearchViewVisible}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid RowDefinitions="80,Auto"
                                  RowSpacing="0">
                                <StackLayout xct:TouchEffect.NativeAnimation="True"
                                             xct:TouchEffect.Command="{Binding Path=BindingContext.OnItemSelectedCommand, Source={x:Reference cv}}"
                                             xct:TouchEffect.CommandParameter="{Binding .}">
                                    <StackLayout Grid.Row="0"
                                                 Spacing="20"
                                                 VerticalOptions="FillAndExpand"
                                                 Orientation="Horizontal"
                                                 Margin="10,0">
                                        <Frame CornerRadius="10"
                                               Padding="0"
                                               HeightRequest="50"
                                               WidthRequest="50"
                                               VerticalOptions="Center"
                                               BackgroundColor="{StaticResource superLightGrayColor}"/>
                                        <controls:CustomLabel Text="{Binding ProductName}"
                                                              VerticalOptions="Center"/>
                                    </StackLayout>
                                </StackLayout>
                                <BoxView Grid.Row="1" 
                                         Color="LightGray" 
                                         HeightRequest="1"/>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Grid>
        </ScrollView>
    </RefreshView>
</ContentPage>